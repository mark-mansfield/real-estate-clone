
    // const textNumValue = parseInt(inputText); /* words and places will evaluate to NaN */
    // let postCode = '';
    // let suburb = '';
    // let australianState = 'string';
    // // Postcode check
    // // check if post code  was entered
    // // post code must be 4 chars and must not evaluate to NaN
    // // we set postcode and state identifier

    // if (inputText.length === 4 && textNumValue !== NaN) {
    //   postCode = textNumValue;
    //   australianState = getStateFromPostCode(textNumValue);
    // }

    // // Australian State check
    // // check if it was a state that was entered
    // // receives -1 for no match or a positive number as an index
    // else if (inputText.length > 1 && inputText.length <= 3) {
    //   let stateString = inputText.toUpperCase();
    //   if (wasStateSelected(stateString) !== -1) {
    //     australianState = stateString;
    //   }
    //   // Suburb check - default cause
    // } else {
    //   suburb = inputText;